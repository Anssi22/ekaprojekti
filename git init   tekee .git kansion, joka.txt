git init   tekee .git kansion, joka tallentaa versioita

git config --global user.name "MAsa Hiko"

git config --global user.email "masa@email.com"

git add lisää 

git commit -m "tää on niinku viesti"

git status   tällä näkee jos on muuttunut jotain  

git commit --amend -m" tehtiin muutoksia lisättii tyylii a committiin b "     




git log ihan kunnon




git restore --staged kolmas.txt                    tällä ujnstagekedetaan, eli poistetaan add eli poistetaan sieltä rekisterista

täätää ku muokkaan ni ei mitään näy




git checkout -- tiedosto.txt                    palautetaan tiedoston viimeisin toimiva versio


git branch    näyttää aikajanat,, esim. master ainaki

git checkout  "haara"               vaihahaara

git merge "PenanHaara"           YHDIUSTÄ PENANHAARA

m modified
u untracked 


git tag -a nimilappumikälaitetaanprojektinaikajanalletiettyykohtgtannta

git switch    vaiha branchi

git show     kattele mikä on se commit missä on turattu jotakin


git revert id-hash peruuttavcatsta commitista

git checkout head x.txt      tällä saa tekstitiedoston takaksi

git branch -m master main              tällä vaihetaan master main

git remote add *nimi *verkkosivu   

git remote -v

git branch -M main

git fetch kurkataan ketä siellä on siellä repositroy hakemiston päässä githubilla

git pull vedetään kamaa